(* The MLI is mostly useless, its main benefit is to reduce the size
of error messages while makeing typing mistakes in the implementation
(the compiler will check it here, instead of within hitcore.mli where
they are all together). *)

include Hitscore_interfaces.B2F_UNALIGNED

